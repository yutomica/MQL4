# MQL4リポジトリ エージェントガイド

## 目的と適用範囲

このファイルは、このディレクトリとその配下で行うMetaTrader 4向けEA、共有インクルード、カスタムインジケーター、および関連MQL4ソースの調査・編集・コンパイルに適用します。

このリポジトリは、統括プロジェクト `C:\Users\yutom\Projects\EABuilder` が管理する外部プロジェクトです。ストラテジーの判断、実験計画、バックテスト結果の評価、およびカスタムエージェント定義はEABuilder側で管理します。

## 統括文書の読込み

すべてのタスクで、作業を開始する前に以下の文書を全文読みます。

1. `C:\Users\yutom\Projects\EABuilder\PROJECT_MAP.md`
2. `C:\Users\yutom\Projects\EABuilder\AGENTS.md`
3. 対象ファイルまでの経路に、より近い `AGENTS.md` または `AGENTS.override.md` が存在する場合は、その文書

外部プロジェクトの場所と役割については `PROJECT_MAP.md` を現在値の正本とし、プロジェクト横断の作業規則と安全上の制約についてはEABuilderの `AGENTS.md` に従います。このファイルはMQL4リポジトリ固有の規則を追加します。

文書間、実際のパス、またはユーザーの依頼内容に矛盾がある場合は、推測で作業を続けず、不一致をユーザーへ報告して解消します。

## リポジトリ構成

- `Experts`：現行EAの主な実装・コンパイル対象です。
- `Experts_old`：旧EAの保管場所です。ユーザーが明示的に指定しない限り、現行EAとして選択または編集してはいけません。
- `Include\Original`：複数EAが利用する共有の本番インクルードです。変更は複数EAへ波及する可能性があります。
- `Indicators`：EAが利用するカスタムインジケーターを含みます。`iCustom` の名前、入力、バッファ番号、およびシフト値と照合します。
- `Presets`：EAやテスターが使用する設定ファイルを含む場合があります。依頼対象でない `.set` を変更してはいけません。
- `Files`、`Logs`：MT4またはEAが生成・利用するデータです。必要な範囲だけを読み、生成物として扱います。
- `Libraries`、`Scripts`、`Projects`、`Shared Projects`：ユーザーが対象へ含めた場合だけ編集します。
- `token.txt`：機密情報を含む可能性があるため、ユーザーが明示的に対象へ含め、かつ作業上不可欠な場合を除き、読み取ったり内容を出力したりしてはいけません。

## 責務の境界

このリポジトリでは、以下を行います。

- MQL4ソースの調査、バグ修正、および承認済みロジックの実装
- 共有インクルードおよびカスタムインジケーターとの整合確認
- 対象EAまたはインジケーターのコンパイルとコンパイラー出力の確認

このリポジトリでは、以下を行いません。

- バックテスト、最適化、または検証の実行管理
- バックテスト結果の集計、採否判断、または改善案の最終決定
- MQL4_execのPython、設定、レポート、または結果ファイルの変更
- ライブ環境へのデプロイまたは注文送信

バックテスト実行と結果取得は `C:\Users\yutom\Projects\MQL4_exec`、評価と改善判断はEABuilderで行います。

## 作業開始時の手順

1. 対象EA、通貨ペア、時間足、変更対象ファイル、承認済み仕様または修正仮説、および変更禁止範囲を確認します。
2. `git status --short --branch` で作業前の状態を確認し、既存のユーザー変更と生成済みファイルを記録します。
3. 対象ファイルの文字コード、BOM、改行形式、および同名の `.ex4` の場所を確認します。
4. `#include`、`iCustom`、共有関数、外部入力、Magic Number、および注文処理を追跡し、関係する呼び出し元と依存先を特定します。
5. `Experts` の現行ファイルと `Experts_old` の旧ファイルを混同していないことを確認します。
6. 同じMQL4ソースを別のエージェントまたはユーザーが編集中でないことを確認します。

## 編集権限と単一ライター規則

- MQL4ソースを編集できるのは、メインCodexエージェント、またはメインエージェントが対象ファイルと変更内容を明示して委任した `mql4_implementer` だけです。
- 書き込み担当は常に1体だけとし、複数エージェントによる同時編集を禁止します。
- `mql4_implementer` の実行中、メインエージェントは同じソースを編集してはいけません。
- `mql4_implementer` が編集できるのは、明示的に指定された `.mq4` または `.mqh` ファイルだけです。
- `mql4_implementer` の完了後、メインエージェントが実際の差分、文字コード、対象範囲、および既存変更との競合を確認してからレビューまたはコンパイルへ進みます。
- ストラテジー批評、MQL4レビュー、および結果分析を担当する読み取り専用サブエージェントにファイルを変更させてはいけません。

## MQL4ソース編集規則

- 承認済み仕様または修正仮説を満たす最小限の差分に限定します。
- 原則として新規にサブルーチンを実装しません。簡潔で分かりやすい実装を行います。
- 関係のないリファクタリング、整形、命名変更、コメント変更、およびパラメータ変更を混在させません。
- 元の文字コード、BOM、改行形式、および既存のコーディング規約を保持します。
- 外部入力変数の名前、型、並び順、初期値を変更する場合は、既存 `.set`、MQL4_execの設定、および比較対象との互換性への影響を確認します。
- `Include\Original` を変更する前に、すべての関係する呼び出し元を検索し、複数EAへのリグレッション範囲を明示します。
- `#include` のファイル名では大文字・小文字の差も確認します。別環境で同一視されると仮定してはいけません。
- `iCustom` を変更または追加する場合は、インジケーター名、入力パラメータの順序、バッファ番号、時間足、およびシフトを実装と照合します。
- `Close[]`、`Open[]`、`High[]`、`Low[]` がチャート時間足に属することと、インジケーターへ渡した時間足を混同してはいけません。
- 確定足を前提とするロジックでは、形成中のバーを参照して先読みやシグナルの変動を起こしていないことを確認します。
- pointとpips、`Digits`、`Point`、価格正規化、スプレッド、ストップレベル、最小ロット、最大ロット、およびロットステップを確認します。
- `OrderSelect`、`OrderSend`、`OrderModify`、`OrderClose`、`OrderDelete` の戻り値、エラー処理、再試行条件、および `RefreshRates` の必要性を確認します。
- SymbolとMagic Numberによる注文の絞り込み、成行・待機注文、部分決済、トレーリングストップ、および時間決済の状態遷移を確認します。
- `OnInit`、`OnDeinit`、`OnTick`、新バー判定、およびグローバル状態の初期化と更新順序を確認します。
- バックテスト時刻、サーバー時刻、ローカル時刻、および曜日・取引時間フィルターの基準を明示します。

## 生成物と禁止対象

- `.ex4`、`.dat`、`.hst`、`.fxt`、`.log`、`.dll`、`mqlcache.dat`、その他のバイナリまたは生成ファイルを直接編集してはいけません。
- `.ex4` の存在や過去の更新日時を、現在の `.mq4` がコンパイル可能である証拠として扱ってはいけません。
- MT4が生成したキャッシュやログを、ユーザーのソース変更と混同してはいけません。
- ファイルの削除、移動、リセット、または旧版への置換は、ユーザーが対象を明示しない限り行いません。

## コンパイル規則

- EA、インジケーター、およびMQL4ソースのコンパイルはメインエージェントだけが行います。`mql4_implementer` を含むサブエージェントに実行させてはいけません。
- このデータフォルダの `origin.txt` が示すインストール先は `C:\Program Files (x86)\Rakuten MetaTrader 4 BackTest` です。コンパイラーを選ぶ場合は、原則として同ディレクトリの `metaeditor.exe` を使用します。
- コンパイル対象には絶対パスを使用し、対象ごとに現在のコンパイラーログを保存または確認します。
- コンパイル前に、対象ファイルと共有インクルードの関数シグネチャを照合します。
- コンパイル後にエラー数、警告数、ログの対象ファイル、および生成された `.ex4` の更新を確認します。
- コンパイルが成功しても、売買ロジックの正しさやバックテスト成功を意味するものとして扱ってはいけません。
- コンパイルによって生成ファイルやキャッシュに差分が生じた場合は、ソース差分と分けて報告します。

## テストへの受け渡し

- コンパイル済みEA名、ソースのGitコミットまたは作業ツリー状態、変更ファイル、成果物の更新日時またはハッシュ、必要な `.set`、対象通貨ペア、時間足、および推奨テスト条件をメインエージェントへ渡します。
- MT4のバックテスト、最適化、および検証はこのリポジトリから直接開始せず、MQL4_execの規則に従って実行します。
- ライブ用ターミナルとバックテスト用ターミナルを混同してはいけません。

## ライブ取引の安全性

- ソース編集とコンパイルは、ライブ取引またはライブ環境へのデプロイを許可するものではありません。
- ユーザーの明示的な承認なしに、AutoTradingを有効化する、ライブチャートへEAを適用する、口座設定を変更する、または注文を送信してはいけません。
- 固定ロット、リスク率、最大ポジション数、ナンピン・ピラミッディング回数、または損失上限を大幅に増やす変更は、実装前に明示的な承認を得ます。

## Gitと変更管理

- 作業前後にGit状態を確認し、既存のユーザー変更を保持します。
- 関係のない変更を破棄、リセット、上書き、ステージング、または取り込んではいけません。
- ユーザーが明示的に依頼しない限り、コミット、プッシュ、タグ付け、ブランチ操作、または履歴書換えを行いません。
- 機密情報、ログ、口座設定、および不要な生成物をGitへ追加してはいけません。

## 完了条件

MQL4の変更は、以下の該当項目を満たした場合にのみ完了とします。

- 実装が承認済み仕様または修正仮説と一致していること。
- 変更対象と差分が明確で、関係のないユーザー変更が保持されていること。
- 文字コードと改行形式が保持されていること。
- 共有コード、外部入力、およびカスタムインジケーターへの影響が確認されていること。
- メインエージェントが現在のソースでコンパイルし、エラーと警告を確認していること。
- 変更ファイル、生成物、検証内容、未確認事項、および次に必要なバックテストが報告されていること。

